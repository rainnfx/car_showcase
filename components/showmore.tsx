"use client";

import { ShowMoreProps } from "@/types";
import { useRouter } from "next/navigation";

const ShowMore = ({ pageNumber, isNext }: ShowMoreProps) => {
  return (
    <div>
      <div>hello me, timestamp: 2:54:44</div>
    </div>
  );
};

export default ShowMore;
